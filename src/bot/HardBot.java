package bot;

/// Predictive (limited lookahead) -- Minimax (depth-limited)
public class HardBot implements Bot {

    @Override
    public int chooseMove(int[] board, int playerFlag) {

        // Logic:
        // Minimax with depth limit (2–3)
        // Use evaluation function for scoring

        return -1; // implement
    }
}